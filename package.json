{
  "name": "cb-zinets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node scripts/filter-published-models.js",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist --no-history --add --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^8.1.4"
  }
}
